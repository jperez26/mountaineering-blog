---
import type { CollectionEntry } from 'astro:content';

interface Props {
  gear: CollectionEntry<'gear'>;
}
const { gear } = Astro.props;
const { data, id } = gear;
const withBase = (href: string) => import.meta.env.BASE_URL.replace(/\/$/, '') + href;
const image = data.image || '/images/placeholders/gear-neutral.svg';
---

<a href={withBase(`/gear/${id}`)} class="group block overflow-hidden rounded-2xl bg-white dark:bg-summit-fern shadow-md ring-1 ring-summit-fern/5 dark:ring-white/10 card-hover">
  <div class="relative h-44 overflow-hidden bg-summit-mist dark:bg-summit-forest/40 flex items-center justify-center">
    <img src={withBase(image)} alt={data.item} class="h-full w-full object-contain p-4 transition-transform duration-500 group-hover:scale-110" loading="lazy" />
  </div>
  <div class="p-5">
    <p class="text-xs font-semibold uppercase tracking-wide text-summit-ember-dark dark:text-summit-ember">{data.category}</p>
    <h3 class="mt-1 font-display text-lg font-bold text-summit-forest dark:text-summit-mist">{data.item}</h3>
    {data.brand && <p class="text-sm text-summit-fern/70 dark:text-summit-mist/80">{data.brand}</p>}
    <div class="mt-2 flex items-center gap-1" aria-label={`${data.rating} out of 5 stars`}>
      {Array.from({ length: 5 }).map((_, i) => (
        <svg class:list={['h-4 w-4', i < data.rating ? 'text-summit-ember' : 'text-summit-fern/20 dark:text-summit-mist/20']} viewBox="0 0 20 20" fill="currentColor">
          <path d="M10 1l2.9 6 6.6.6-5 4.5 1.5 6.5-6-3.6-6 3.6 1.5-6.5-5-4.5 6.6-.6z" />
        </svg>
      ))}
    </div>
  </div>
</a>
