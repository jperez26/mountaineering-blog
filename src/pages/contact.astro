---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';

const FORMSPREE_ID = 'xkodekkv';
const formAction = `https://formspree.io/f/${FORMSPREE_ID}`;
const isConfigured = FORMSPREE_ID !== 'YOUR_FORM_ID';
const contactEmail = 'jono2perez@gmail.com';
---

<BaseLayout title="Contact" description="Get in touch.">
  <Hero image="/images/climbs/iztaccihuatl.jpg" eyebrow="Say Hello" title="Get in Touch" subtitle="Questions about a route, gear, or just want to say hi?" size="md" />

  <section class="mx-auto max-w-2xl px-4 sm:px-6 py-20 reveal">
    {!isConfigured && (
      <div class="mb-8 rounded-xl border border-summit-ember/40 bg-orange-50 px-5 py-4 text-sm text-summit-forest">
        <strong>Heads up:</strong> this form isn't wired up yet. Create a free account at
        <a href="https://formspree.io" target="_blank" rel="noopener" class="underline font-semibold">formspree.io</a>,
        grab your form ID, and replace <code>YOUR_FORM_ID</code> in <code>src/pages/contact.astro</code>. Full steps are in
        <code>SETUP.md</code>. Until then, reach out directly at
        <a href={`mailto:${contactEmail}`} class="underline font-semibold">{contactEmail}</a>.
      </div>
    )}

    <form action={formAction} method="POST" class="space-y-5 rounded-2xl bg-white dark:bg-summit-fern shadow-md ring-1 ring-summit-fern/5 dark:ring-white/10 p-6 sm:p-8">
      <input type="text" name="_gotcha" class="hidden" tabindex="-1" autocomplete="off" />

      <div>
        <label for="name" class="block text-sm font-semibold text-summit-forest dark:text-summit-mist">Name</label>
        <input
          id="name"
          name="name"
          type="text"
          required
          class="mt-1 w-full rounded-lg border border-summit-fern/20 dark:border-white/15 bg-white dark:bg-summit-forest/40 px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-summit-moss"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-semibold text-summit-forest dark:text-summit-mist">Email</label>
        <input
          id="email"
          name="email"
          type="email"
          required
          class="mt-1 w-full rounded-lg border border-summit-fern/20 dark:border-white/15 bg-white dark:bg-summit-forest/40 px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-summit-moss"
        />
      </div>

      <div>
        <label for="message" class="block text-sm font-semibold text-summit-forest dark:text-summit-mist">Message</label>
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          class="mt-1 w-full rounded-lg border border-summit-fern/20 dark:border-white/15 bg-white dark:bg-summit-forest/40 px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-summit-moss"
        ></textarea>
      </div>

      <button
        type="submit"
        class="rounded-full bg-summit-ember px-6 py-3 font-semibold text-summit-forest hover:bg-orange-400 transition-colors"
      >
        Send Message
      </button>
    </form>
  </section>
</BaseLayout>
